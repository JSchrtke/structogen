name("template name")

instruction("counter = 0")

while ("counter != 10") {
    instruction("print counter")

    if ("counter % 2 == 0") {
        call("printEven()")
    } else {
        call("printOdd()")
    }

    instruction("counter++")

    dowhile("counter < 5") {
        switch("counter") {
            {1:"printOne"}
            {2:"printTwo"}
            {3:""}
            {default:"printDefault"}
        }

        instruction("counter++")
    }
}
